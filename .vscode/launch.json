{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-extensionHost",
      "request": "launch",
      "name": "Run Commit Message Extension",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
    }
  ]
}
